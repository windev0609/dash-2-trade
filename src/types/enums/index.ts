export enum TextColorEnum {
  Green = " text-green",
  Red = " text-red",
  Blue = " text-button-primary-highlight",
  Neutral = "",
}

export enum TokenSortEnum {
  Id = "id",
  Volume = "volume",
  PriceChange = "price_change",
  MarketCap = "market_cap",
}

export enum TokenSortOrdersEnum {
  Asc = "asc",
  Desc = "desc",
}

export enum sentimentTitleEnum {
  neu = "Neutral",
  neg = "Negative",
  pos = "Positive",
}

export enum sentimentClassEnum {
  neg = "text-red",
  pos = "text-green",
}

export enum PresaleFilterOptionsEnum {
  kyc = "KYC",
  audit = "Audit",
  vc = "VC",
  active = "Active",
}

export enum PresaleTokenStatusEnum {
  active = "active",
  incoming = "incoming",
  inprogress = "inprogress",
  ended = "ended",
  cancelled = "cancelled",
}
